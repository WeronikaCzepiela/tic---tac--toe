import styled from "styled-components";

export const BottomStyled = styled.header`
  display: flex;
  flex-direction: row;
  align-items: center;
  font-size: 20px;

  .icons {
    padding-left: 13px;
    gap: 10px
  }
`